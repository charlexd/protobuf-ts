type Long = protobuf.Long;

/** Namespace game. */
declare namespace game {

    /** MsgType enum. */
    enum MsgType {
        UNKNOWN_MSG = 1
    }

    /** E_ErrorCode enum. */
    enum E_ErrorCode {
        NO_ERROR = 0,
        LOGIN_ON_OTHER_DEVICE = 1
    }
}
